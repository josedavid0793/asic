import { Title } from '@angular/platform-browser';

export interface Host_clientes{
   	
         id?: number;
         host_name:string;
		 ip: string;
		 cliente: string;
		 ambiente: string;
		 sistema_operativo: string;
		 escalamiento : string;
		 notas : string;
}